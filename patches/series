opencl-vision/0001-mtmd-qwen-patch-embedding-as-gemm.patch
opencl-vision/0002-mtmd-clip-compute-warmup.patch
opencl-vision/0003-mtmd-qwen3vl-zero-mask-test-aid.patch
hexagon-fusion/0001-hexagon-htp-fused-matvec-add-read-src2-from-ddr.patch
hexagon-fusion/0002-hexagon-htp-fused-gdn-state-step.patch
hexagon-fusion/0003-hexagon-htp-gdn-state-step-hvx-conv-and-match-fix.patch
hexagon-vit/0001-hexagon-gelu-tanh-form.patch
llama-tied-head/0001-qwen35-tied-head-output-rot.patch
hexagon-fusion/0004-hexagon-htp-gdn-slot-index-from-host-and-error-report.patch
llama-tied-head/0002-qwen35-mtp-rotation.patch
mtmd/0001-mtmd-qwen3vl-position-embedding-on-cpu.patch
mtmd/0002-mtmd-qwen3vl-one-rope-for-q-and-k.patch
hexagon-host/0001-hexagon-host-timers-and-batch-cache.patch
hexagon-host/0002-llama-host-timers-and-embd-lookup.patch
hexagon-host/0003-llama-sampler-topset.patch
hexagon-fusion/0005-hexagon-htp-gdn-chunked-hmx.patch
hexagon-multirow/0001-hexagon-htp-multirow-decode-step.patch
hexagon-fusion/0006-hexagon-htp-gdn-conv-chunk.patch
# The chunked delta rule above lands at an error of 1.24e-7 against the usual
# limit of 1.0e-7 in test-backend-ops: the rounding of the fp16 inputs of the
# HMX. Patch 0006 sets the limit to 1e-5 for the HTP backend on the chunkable
# shapes only, and the other backends and shapes keep 1e-7. The model does not
# move: the 2B Q8_0 file on the phone gives maximum KL 0.00986 and top-1
# 99.35 % with the kernel and 0.00983 and 99.22 % without it, and the prefill
# goes from 981 to 1675 t/s.

hexagon-fusion/0007-hexagon-htp-gdn-fusions-with-rollback-slots.patch

# The f16 math helpers, before any kernel moves to f16. hvx_vec_exp2_f16 held a raw qf16 constant
# that hexagon-clang 19.0.07 reads as an IEEE half when it inlines the function into a larger loop
# (2 % to 56 % error in exp2), and hvx_vec_fast_sigmoid_f16 cancelled for a negative input (29 % rms
# over [-12, 12]). tools/htp-lab target f16math measures the two. The library of the app did not
# hold the wrong multiply, thus no shipped result changes.
hexagon-f16/0001-hexagon-f16-exp2-coefficient-and-sigmoid-branch.patch

# The kernel work of 2026-09-20. Each patch is measured in tools/htp-lab against a
# float64 reference, in the cycle-accurate simulator, and its header carries the
# cycles and the error before and after.
#
# THREE OF THEM GAVE A WRONG MODEL ON THE DEVICE AND THE LAB DID NOT SEE IT.
# On 2026-09-20 the NPU answered every prompt with one repeated token, while the
# CPU and the GPU were correct. A bisect on the phone, one library for each
# prefix, isolated 0004 and 0011, and an audit then found a third defect in 0010.
# Each one is now repaired and each patch header holds its mechanism:
#   0004  the Horner seed was an IEEE single that the next step read as a qf32
#   0010  the int16 rows dropped the tail, and the SIGMOID row is 1 element
#   0001  the int16 SiLU returned a negative h, and an unsigned exponent
#         subtract then turned it into +Inf. 0011 only selects that path.
#
# Why the lab passed them: it measures one kernel that it compiles on its own,
# with a token count that is a multiple of 4 vectors, against a float64
# reference. The library compiles the same source with -flto into a translation
# unit of a different shape, and the model calls it with ne0 = the head count.
# A lab result is thus necessary and not sufficient: every kernel patch needs
# one generation on the device before it goes into this file.

hexagon-kernels/0001-hexagon-htp-gdn-conv-chunk-packed-band.patch
hexagon-kernels/0002-hexagon-htp-gdn-chunk-rank1-decay-and-transpose.patch
hexagon-kernels/0003-hexagon-htp-activation-int16-tables.patch
hexagon-kernels/0004-hexagon-htp-softplus-on-the-vector-unit.patch
hexagon-kernels/0005-hexagon-htp-gdn-conv-step-four-chains.patch
hexagon-kernels/0006-hexagon-htp-arith-fused-chains.patch
hexagon-kernels/0007-hexagon-htp-softmax-and-norm-packed.patch
hexagon-kernels/0008-hexagon-htp-flash-attn-code-size.patch
hexagon-kernels/0009-hexagon-htp-cpy-transpose-tiles.patch

hexagon-kernels/0010-hexagon-htp-wire-activation-tables.patch

# The int16 activation kernels of 0003 compute nvec = n / 32 and drop the rest of
# the row. Qwen3.5 gives the SIGMOID a row of 1 element, thus the kernel wrote
# nothing and the gate held the previous contents of the scratch pad. This patch
# adds the tail store. It comes after 0010, which is the tree it was made against.
hexagon-kernels/0003b-hexagon-htp-act-i16-tail-store.patch

hexagon-kernels/0011-hexagon-htp-gdn-conv-chunk-band-by-default.patch

# The HMX 2D path pipelines below 33 rows. The threshold was an untested guess:
# the VTCM budget allows the pipelined layout at every row count from 5 to 32.
hexagon-mm/0001-hexagon-mm-pipeline-below-33-rows.patch

# Speculative decoding gives the context K rollback slots. The chunked gated
# delta net kernel refused K > 1, thus prefill with speculation on ran the
# sequential kernel and lost a factor of 2.1.
hexagon-spec/0001-hexagon-htp-gdn-chunked-and-conv-rollback-slots.patch

# A prompt ubatch has nothing to roll back. A ubatch that holds more tokens of
# one sequence than the rollback depth cannot be a verify step, thus it takes
# one snapshot group and its graph equals the graph of speculation off.
hexagon-spec/0002-llama-bound-the-recurrent-rollback-by-the-last-ubatch.patch
hexagon-spec/0003-llama-one-recurrent-snapshot-group-for-a-prompt-ubatch.patch

# The drafter reads a frequency-ranked subset of the head. A model without the
# three tensors takes the full head, thus the patch is inert for a plain GGUF.
draft-head/0001-llama-qwen35-reduced-mtp-draft-head.patch

# The int8 HMX macros. No kernel uses them yet, thus the patch is inert.
hexagon-hmx-i8/0001-hexagon-hmx-int8-macros.patch
hexagon-repeat-splat/0001-hexagon-htp-splat-the-scalar-broadcast-of-repeat.patch
spec-draft-4/0001-common-default-the-draft-depth-to-4.patch
