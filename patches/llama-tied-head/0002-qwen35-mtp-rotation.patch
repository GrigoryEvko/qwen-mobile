diff --git a/conversion/qwen.py b/conversion/qwen.py
index c7e0809..c21b447 100644
--- a/conversion/qwen.py
+++ b/conversion/qwen.py
@@ -329,6 +329,9 @@ class _QwenMtpMixin:
                 "pre_fc_norm_embedding": "enorm",
                 "pre_fc_norm_hidden":    "hnorm",
                 "norm":                  "shared_head.norm",
+                # the two dense maps of a rotated checkpoint (the quantization pipeline)
+                "hnorm_rot":             "hnorm_rot",
+                "shared_head_rot":       "shared_head_rot",
             }
             parts = name.split(".", 3)
             if len(parts) == 4 and parts[1] == "layers" and parts[2].isdecimal():
diff --git a/gguf-py/gguf/constants.py b/gguf-py/gguf/constants.py
index a309086..4028299 100644
--- a/gguf-py/gguf/constants.py
+++ b/gguf-py/gguf/constants.py
@@ -1194,6 +1194,8 @@ class MODEL_TENSOR(IntEnum):
     NEXTN_HNORM            = auto()
     NEXTN_SHARED_HEAD_HEAD = auto()
     NEXTN_SHARED_HEAD_NORM = auto()
+    NEXTN_HNORM_ROT        = auto() # qwen35 rotated checkpoint: a dense map before hnorm
+    NEXTN_SHARED_HEAD_ROT  = auto() # qwen35 rotated checkpoint: a dense map after shared_head_norm
     # eagle3
     FC                     = auto()  # feature fusion layer
     D2T                    = auto()  # draft to target vocabulary mapping
@@ -1981,6 +1983,8 @@ TENSOR_NAMES: dict[MODEL_TENSOR, str] = {
     MODEL_TENSOR.NEXTN_HNORM:               "blk.{bid}.nextn.hnorm",
     MODEL_TENSOR.NEXTN_SHARED_HEAD_HEAD:    "blk.{bid}.nextn.shared_head_head",
     MODEL_TENSOR.NEXTN_SHARED_HEAD_NORM:    "blk.{bid}.nextn.shared_head_norm",
+    MODEL_TENSOR.NEXTN_HNORM_ROT:           "blk.{bid}.nextn.hnorm_rot",
+    MODEL_TENSOR.NEXTN_SHARED_HEAD_ROT:     "blk.{bid}.nextn.shared_head_rot",
     MODEL_TENSOR.FC:                        "fc",
     MODEL_TENSOR.DSPARK_MARKOV_W1:          "markov_w1",
     MODEL_TENSOR.DSPARK_MARKOV_W2:          "markov_w2",
@@ -2889,6 +2893,8 @@ MODEL_TENSORS: dict[MODEL_ARCH, list[MODEL_TENSOR]] = {
         MODEL_TENSOR.NEXTN_HNORM,
         MODEL_TENSOR.NEXTN_SHARED_HEAD_HEAD,
         MODEL_TENSOR.NEXTN_SHARED_HEAD_NORM,
+        MODEL_TENSOR.NEXTN_HNORM_ROT,
+        MODEL_TENSOR.NEXTN_SHARED_HEAD_ROT,
     ],
     MODEL_ARCH.QWEN35MOE: [
         MODEL_TENSOR.TOKEN_EMBD,
diff --git a/gguf-py/gguf/tensor_mapping.py b/gguf-py/gguf/tensor_mapping.py
index 2f1b761..65c4a58 100644
--- a/gguf-py/gguf/tensor_mapping.py
+++ b/gguf-py/gguf/tensor_mapping.py
@@ -2760,6 +2760,14 @@ class TensorNameMap:
         MODEL_TENSOR.NEXTN_SHARED_HEAD_NORM: (
             "model.layers.{bid}.shared_head.norm",
         ),
+
+        MODEL_TENSOR.NEXTN_HNORM_ROT: (
+            "model.layers.{bid}.hnorm_rot",  # qwen35 rotated checkpoint
+        ),
+
+        MODEL_TENSOR.NEXTN_SHARED_HEAD_ROT: (
+            "model.layers.{bid}.shared_head_rot",  # qwen35 rotated checkpoint
+        ),
     }
 
     # architecture-specific block mappings
diff --git a/src/llama-arch.cpp b/src/llama-arch.cpp
index 9b9bedc..c135a4d 100644
--- a/src/llama-arch.cpp
+++ b/src/llama-arch.cpp
@@ -586,6 +586,8 @@ static const std::map<llm_tensor, const char *> LLM_TENSOR_NAMES = {
     { LLM_TENSOR_NEXTN_HNORM,                            "blk.%d.nextn.hnorm" },
     { LLM_TENSOR_NEXTN_SHARED_HEAD_HEAD,                 "blk.%d.nextn.shared_head_head" },
     { LLM_TENSOR_NEXTN_SHARED_HEAD_NORM,                 "blk.%d.nextn.shared_head_norm" },
+    { LLM_TENSOR_NEXTN_HNORM_ROT,                        "blk.%d.nextn.hnorm_rot" },
+    { LLM_TENSOR_NEXTN_SHARED_HEAD_ROT,                  "blk.%d.nextn.shared_head_rot" },
     { LLM_TENSOR_ATTN_SUB_NORM,                          "blk.%d.attn_sub_norm" },
     { LLM_TENSOR_FFN_SUB_NORM,                           "blk.%d.ffn_sub_norm" },
     { LLM_TENSOR_DEC_OUTPUT_NORM,                        "dec.output_norm" },
@@ -977,6 +979,8 @@ static const std::map<llm_tensor, llm_tensor_info> LLM_TENSOR_INFOS = {
     {LLM_TENSOR_NEXTN_HNORM,                {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL}},
     {LLM_TENSOR_NEXTN_SHARED_HEAD_HEAD,     {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
     {LLM_TENSOR_NEXTN_SHARED_HEAD_NORM,     {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL}},
+    {LLM_TENSOR_NEXTN_HNORM_ROT,            {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
+    {LLM_TENSOR_NEXTN_SHARED_HEAD_ROT,      {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
     // Nemotron 3 Super
     // latent projections feed ggml_mul_mat, the buft probe must use MUL_MAT to keep them on GPU
     {LLM_TENSOR_FFN_LATENT_DOWN,            {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
diff --git a/src/llama-arch.h b/src/llama-arch.h
index c780783..d5aacdb 100644
--- a/src/llama-arch.h
+++ b/src/llama-arch.h
@@ -697,6 +697,8 @@ enum llm_tensor {
     LLM_TENSOR_NEXTN_HNORM,
     LLM_TENSOR_NEXTN_SHARED_HEAD_HEAD,
     LLM_TENSOR_NEXTN_SHARED_HEAD_NORM,
+    LLM_TENSOR_NEXTN_HNORM_ROT,       // a dense map before hnorm, for a rotated residual stream
+    LLM_TENSOR_NEXTN_SHARED_HEAD_ROT, // a dense map after shared_head_norm, for a rotated residual stream
     LLM_TENSOR_MASKED_EMBD_CENTROIDS,
     LLM_TENSOR_MASKED_EMBD_ORDERING,
     LLM_TENSOR_HRM_Z_L_INIT,
diff --git a/src/llama-model.h b/src/llama-model.h
index d86a4a7..5b6294f 100644
--- a/src/llama-model.h
+++ b/src/llama-model.h
@@ -231,6 +231,11 @@ struct llama_layer_nextn {
     struct ggml_tensor * shared_head_head_s    = nullptr;
     struct ggml_tensor * shared_head_head_in_s = nullptr;
     struct ggml_tensor * shared_head_norm      = nullptr;
+    // a rotated residual stream (the quantization pipeline): the map before hnorm makes the
+    // final-norm output of the original model, the map after shared_head_norm puts the state of
+    // the block into the convention of the h_nextn of the main graph
+    struct ggml_tensor * hnorm_rot             = nullptr;
+    struct ggml_tensor * shared_head_rot       = nullptr;
 };
 
 struct llama_layer_switch_lora {
diff --git a/src/models/qwen35.cpp b/src/models/qwen35.cpp
index 4a2febd..8fc47bf 100644
--- a/src/models/qwen35.cpp
+++ b/src/models/qwen35.cpp
@@ -117,6 +117,9 @@ void llama_model_qwen35::load_arch_tensors(llama_model_loader & ml) {
         layer.nextn.embed_tokens     = create_tensor(tn(LLM_TENSOR_NEXTN_EMBED_TOKENS,     "weight", il), { n_embd, n_vocab },     mtp_flags|TENSOR_NOT_REQUIRED);
         layer.nextn.shared_head_head = create_tensor(tn(LLM_TENSOR_NEXTN_SHARED_HEAD_HEAD, "weight", il), { n_embd, n_vocab },     mtp_flags|TENSOR_NOT_REQUIRED);
         layer.nextn.shared_head_norm = create_tensor(tn(LLM_TENSOR_NEXTN_SHARED_HEAD_NORM, "weight", il), { n_embd },              mtp_flags|TENSOR_NOT_REQUIRED);
+        // the two dense maps of a rotated checkpoint, which a plain conversion does not have
+        layer.nextn.hnorm_rot        = create_tensor(tn(LLM_TENSOR_NEXTN_HNORM_ROT,        "weight", il), { n_embd, n_embd },      mtp_flags|TENSOR_NOT_REQUIRED);
+        layer.nextn.shared_head_rot  = create_tensor(tn(LLM_TENSOR_NEXTN_SHARED_HEAD_ROT,  "weight", il), { n_embd, n_embd },      mtp_flags|TENSOR_NOT_REQUIRED);
     };
 
     for (int i = 0; i < n_layer; ++i) {
@@ -546,6 +549,13 @@ llama_model_qwen35::graph_mtp::graph_mtp(const llama_model & model, const llm_gr
 
     auto * inp_attn = build_attn_inp_kv();
 
+    if (layer.nextn.hnorm_rot) {
+        // a rotated checkpoint supplies the normed hidden state in the rotated basis: the dense map
+        // makes the final-norm output of the original model from it, which hnorm reads
+        h_embd = ggml_mul_mat(ctx0, layer.nextn.hnorm_rot, h_embd);
+        cb(h_embd, "mtp_hnorm_rot", il);
+    }
+
     ggml_tensor * h_norm = build_norm(h_embd, layer.nextn.hnorm, nullptr, LLM_NORM_RMS, il);
     cb(h_norm, "mtp_hnorm", il);
 
@@ -636,16 +646,28 @@ llama_model_qwen35::graph_mtp::graph_mtp(const llama_model & model, const llm_gr
     GGML_ASSERT(head_norm_w && "QWEN35 MTP: missing both nextn.shared_head_norm and output_norm");
     cur = build_norm(cur, head_norm_w, nullptr, LLM_NORM_RMS, -1);
 
+    if (layer.nextn.shared_head_rot) {
+        // a rotated checkpoint: the dense map puts the normed state of the block into the convention
+        // of the h_nextn of the main graph, thus the next draft step and the head path that follows
+        // read it as they read the main model
+        cur = ggml_mul_mat(ctx0, layer.nextn.shared_head_rot, cur);
+        cb(cur, "mtp_shared_head_rot", -1);
+    }
+
     cb(cur, "h_nextn", -1);
     res->t_h_nextn = cur;
 
     cur = ggml_get_rows(ctx0, cur, inp_out_ids);
     cb(cur, "mtp_shared_head_norm", -1);
 
-    // the MTP head does not apply model.output_rot: the transformed checkpoints do not keep the MTP block exact
     ggml_tensor * head_w = layer.nextn.shared_head_head ? layer.nextn.shared_head_head : model.output;
     ggml_tensor * head_s = layer.nextn.shared_head_head ? layer.nextn.shared_head_head_s : model.output_s;
     GGML_ASSERT(head_w && "QWEN35 MTP: missing LM head (nextn.shared_head_head or model.output)");
+    if (!layer.nextn.shared_head_head && model.output_rot) {
+        // the shared head is the tied head of the main graph, which reads the state through the dense map
+        cur = ggml_mul_mat(ctx0, model.output_rot, cur);
+        cb(cur, "mtp_output_rot", -1);
+    }
     cur = build_lora_mm(head_w, cur, head_s);
     cb(cur, "result_output", -1);
 
